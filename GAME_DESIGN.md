# 게임 개발 프로젝트 기획서 (v3 — 증강형 빌드 시스템 추가)

> 퇴근 후 하루 1시간 남짓 틈틈이 진행. 날짜 고정 없이 마일스톤 단위로 진행합니다.

역할 분담: 범석님 = 게임 서버(C# TCP), Claude = Unity 클라이언트(2D)

## 1. 통신 프로토콜 방향 — 커스텀 TCP

Mirror 같은 고수준 네트워킹 프레임워크는 클라이언트 개발 속도는 빠르지만, 소켓 관리·패킷 직렬화·상태 동기화를 프레임워크가 대신 처리해줘서 "게임 서버 프로그래머"로서 보여줄 수 있는 게 적어집니다. 범석님이 이미 갖고 있는 C# 비동기 TCP 서버 경험을 그대로 확장하는 **커스텀 TCP 프로토콜**로 갑니다. 하루 1시간씩 짬짬이 진행하는 만큼, 패킷 포맷은 최대한 단순하게 유지합니다.

## 2. 게임 컨셉

**가칭: 라스트 원 스탠딩 — 2D 탑다운 증강형 미니 아레나 대전**

기본 골격(2~4인 실시간 대전, 권위 서버 구조)에 **LoL 아레나 모드의 "증강체(augment)" 시스템**을 얹은 형태로 확정합니다. 매치 시작 시 캐릭터/스타일을 고르고, 라운드가 진행될수록 증강을 골라 쌓아가면서 각자 다른 빌드를 만들어가는 방식이에요.

### 2.1 코어 루프

```
매치 시작
 └─ 시작 캐릭터/스타일 선택 (전원 동시)
    └─ [라운드 반복]
         1) 라운드 시작: 아레나에 스폰, 현재까지 쌓은 증강 효과 적용된 상태
         2) 제한시간 내 전투 → 마지막 생존자 또는 최다 데미지/킬이 라운드 승리
         3) 라운드 종료 → 전원에게 증강 후보 3개 제시 → 1개 선택
    └─ (3라운드 반복 후 종료)
 └─ 매치 종료: 누적 라운드 승수 최다 플레이어가 최종 승리 (동점 시 서든데스 라운드)
```

### 2.2 시작 캐릭터/스타일 선택

매치 시작 시 2~3개의 시작 스타일 중 하나를 고릅니다. 유닛 자체를 바꾸기보다는 **시작 스탯 배분**만 다르게 해서 범위를 좁게 유지합니다 (예시, 조정 가능):

| 스타일 | 특징 |
|---|---|
| 전사형 | 근접 공격, 데미지 高, 이동속도 보통 |
| 궁수형 | 원거리 공격, 데미지 中, 사거리 高 |
| 탱커형 | 체력 高, 이동속도 低 |

### 2.3 증강(특성) 시스템

라운드 종료마다 무작위 3개 후보 중 1개를 골라 영구 적용합니다. MVP 단계에서는 풀을 5~8개 정도로 최소화하고, 이후 늘려갑니다.

예시 풀:
- 공격력 +X%
- 이동속도 +X%
- 최대체력 +X
- 공격 적중 시 일정 확률로 체력 회복(라이프스틸)
- 공격 사거리/범위 증가
- 일정 쿨다운마다 대시(회피기) 사용 가능

증강 효과 계산과 검증은 **서버가 권위를 가지고** 처리하고, 클라이언트는 결과(스탯, 효과)를 받아 표시만 합니다 — 부정행위 방지 및 서버 개발 포트폴리오 관점에서 중요한 부분입니다.

### 2.4 승리 조건

우선 3라운드로 짧게 구성합니다. 3라운드 동안 누적 라운드 승수가 가장 많은 플레이어가 매치 승리. 동점이면 서든데스 라운드 1회 추가.

이 컨셉 자체나 세부 수치(라운드 수, 증강 목록, 시작 스타일)는 언제든 조정 가능합니다 — 지금은 뼈대만 잡아둔 거예요.

## 3. 통신 프로토콜 초안

패킷 포맷 (단순화를 위해 JSON 페이로드 사용, 필요시 이후 바이너리로 최적화):

```
[4바이트: payload 길이(빅엔디안)] + [2바이트: 패킷 ID] + [payload(UTF-8 JSON)]
```

### 로비 단계
| 패킷 ID | 방향 | 내용 |
|---|---|---|
| JOIN_LOBBY | C→S | 닉네임 |
| LOBBY_JOINED | S→C | 플레이어ID, 현재 방 목록 |
| CREATE_ROOM | C→S | 방 이름 |
| JOIN_ROOM | C→S | 방 ID |
| ROOM_STATE | S→C | 방 내 플레이어 목록, 각자 ready 상태 |
| READY | C→S | ready 토글 |

### 매치 설정 / 캐릭터 선택
| 패킷 ID | 방향 | 내용 |
|---|---|---|
| MATCH_CONFIG | S→C | 총 라운드 수, 시작 스타일 후보 목록 |
| SELECT_STARTER | C→S | 선택한 시작 스타일 ID |
| MATCH_START | S→C | 카운트다운(초), 각 플레이어 시작 스탯 |

### 인게임(라운드 전투)
| 패킷 ID | 방향 | 내용 |
|---|---|---|
| ROUND_START | S→C | 라운드 번호, 스폰 좌표, 각 플레이어 현재 누적 증강/스탯 |
| MOVE_INPUT | C→S | 방향/좌표, 클라이언트 타임스탬프 |
| STATE_SNAPSHOT | S→C | 전 플레이어 위치/체력, 서버 tick |
| ATTACK | C→S | 방향 또는 타겟 ID |
| ATTACK_RESULT | S→C | 공격자ID, 피격자ID, 데미지 |
| PLAYER_DEATH | S→C | 플레이어ID |
| ROUND_RESULT | S→C | 라운드 승자ID, 누적 라운드 스코어 |

### 증강 선택
| 패킷 ID | 방향 | 내용 |
|---|---|---|
| AUGMENT_OFFER | S→C | 증강 후보 3개(ID, 이름, 설명, 효과값) |
| AUGMENT_PICK | C→S | 선택한 증강 ID |
| BUILD_UPDATE | S→C | 플레이어별 누적 빌드 상태(UI 표시용) |

### 매치 종료 / 기타
| 패킷 ID | 방향 | 내용 |
|---|---|---|
| MATCH_END | S→C | 최종 순위, 각 플레이어 최종 빌드 |
| PING / PONG | C↔S | 레이턴시 측정 |
| PLAYER_DISCONNECTED | S→C | 플레이어ID |

이 스펙을 기준으로 서버/클라이언트를 병렬 개발하면 중간에 안 맞는 부분을 최소화할 수 있어요. 필드명, 좌표계, 증강 수치 등은 확정되는 대로 이 문서를 갱신합니다.

### 3.1 패킷 ID 값 (2바이트, 카테고리별로 여유 확보)

| 값 | 패킷 |
|---|---|
| 1 | JOIN_LOBBY |
| 2 | LOBBY_JOINED |
| 3 | CREATE_ROOM |
| 4 | JOIN_ROOM |
| 5 | ROOM_STATE |
| 6 | READY |
| 10 | MATCH_CONFIG |
| 11 | SELECT_STARTER |
| 12 | MATCH_START |
| 20 | ROUND_START |
| 21 | MOVE_INPUT |
| 22 | STATE_SNAPSHOT |
| 23 | ATTACK |
| 24 | ATTACK_RESULT |
| 25 | PLAYER_DEATH |
| 26 | ROUND_RESULT |
| 30 | AUGMENT_OFFER |
| 31 | AUGMENT_PICK |
| 32 | BUILD_UPDATE |
| 40 | MATCH_END |
| 41 | PING |
| 42 | PONG |
| 43 | PLAYER_DISCONNECTED |

카테고리마다 번호 사이 여유를 둬서 나중에 패킷이 추가돼도 기존 번호가 안 밀리게 했습니다.

### 3.2 접속 정보 (임시)

- host: `127.0.0.1` (로컬 테스트 기준, 나중에 실제 배포 시 변경)
- port: `9000`

편하신 값으로 바꾸셔도 됩니다 — 바꾸시면 알려주세요, 목업 클라이언트/Unity 쪽도 맞출게요.

### 3.3 서버 단독 테스트용 목업 클라이언트

Unity 클라이언트가 준비되기 전에도 서버 로직을 테스트할 수 있도록 파이썬 목업 클라이언트(`Tools/mock_client.py`)를 준비해뒀습니다. 콘솔에서 패킷 이름 + JSON payload를 입력하면 위 스펙대로 프레이밍해서 전송하고, 서버 응답도 실시간으로 출력합니다. 사용법은 `Tools/README.md` 참고.

## 4. 마일스톤 (1시간 세션 기준 대략치)

날짜는 고정하지 않고, 각 마일스톤이 끝나면 다음으로 넘어갑니다. 서버/클라이언트 각자 편한 속도로 진행하고, 마일스톤 경계에서 프로토콜이 맞는지만 서로 확인하면 됩니다.

| 마일스톤 | 대략 세션 수(1h 기준) | 서버(범석님) | 클라이언트(Claude) |
|---|---|---|---|
| M1. 셋업 & 스펙 확정 | 1세션 | 프로젝트 구조 셋업, 프로토콜 스펙 확정 | Unity 프로젝트 생성, 씬 구성, 프로토콜 스펙 확정 |
| M2. 로비/룸 시스템 | 2~3세션 | 룸 매니저 구현 (생성/참가/ready) | 로비 UI + 방 생성/참가 화면 |
| M3. 실시간 이동 동기화 | 2~3세션 | tick 루프, 위치 브로드캐스트 | 이동 입력 전송 + 위치 보간(interpolation) |
| M4. 전투 로직 | 2세션 | 공격 판정/데미지 처리 | 공격 애니메이션/이펙트, 체력 UI |
| M5. 라운드 구조 & 증강 시스템 | 2~3세션 | 라운드 매니저, 증강 풀/효과 적용 로직 | 시작 스타일/증강 선택 UI, 라운드 결과 화면 |
| M6. 매치 종료 & 승리 조건 | 1세션 | 누적 스코어/서든데스 판정 | 최종 결과/빌드 요약 화면 |
| M7. 안정화 | 2세션 | 재접속/타임아웃/disconnect 처리 | 통합 테스트, 예외 UI 처리 |
| M8. 폴리싱 & 정리 | 1~2세션 | 폴리싱, 통합 테스트 | 데모 녹화, README/아키텍처 다이어그램 정리 |

M2부터는 서버/클라이언트를 병렬로 진행할 수 있으니, 그날그날 어디까지 하셨는지만 알려주시면 그에 맞춰 클라이언트 쪽 진행 상황을 맞출게요.

## 5. 첫 세션 할 일

1. 이 기획서(특히 증강 시스템, 라운드 수, 시작 스타일)에 대한 피드백
2. 서버: 프로젝트 구조 셋업 (네임스페이스, 룸/플레이어 클래스 뼈대)
3. 클라이언트(Claude): Unity 프로젝트 생성 + 기본 씬(로비/인게임) 구성 시작

## 6. 진행 로그

- **9/13**: 기획/프로토콜 확정(증강 시스템, 패킷 ID, 3라운드). 프로젝트 폴더 구조 생성. 서버 솔루션(GameServer.sln, Server.App/Network/Game/Shared) 스캐폴드 — 구조/시그니처만, 구현은 TODO. 목업 클라이언트(`Tools/mock_client.py`) 준비.
- **9/14**: 클라이언트 네트워크 레이어 구현 — `Client/Assets/_Project/Scripts/Network/`에 `PacketIds.cs`, `PacketFramer.cs`, `NetworkClient.cs` 작성 완료(서버와 동일한 패킷 포맷, 백그라운드 스레드 읽기 + 메인 스레드 디스패치). 서버 측은 `PacketFramer.cs` 등 구현 예정.
